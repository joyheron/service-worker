{
  "name": "service-worker",
  "version": "1.0.0",
  "description": "Playground for testing service workers",
  "scripts": {
    "start": "npm run compile:watch",
    "compile:watch": "faucet --watch",
    "compile": "faucet --compact",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "faucet-pipeline-js": "^2.1.7"
  },
  "dependencies": {
    "parse5": "^6.0.1"
  }
}
